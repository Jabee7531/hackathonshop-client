import { css } from "@emotion/react";
import CartPage from "../../components/cartPade/CartPage";

const Index = () => {
  return (
    <div css={indexStyle}>
      <CartPage />
    </div>
  );
};

export default Index;

const indexStyle = css``;
